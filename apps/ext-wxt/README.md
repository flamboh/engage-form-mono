# Engage Form WXT Extension

Temporary WXT migration package for the Engage Form browser extension.

The current `apps/extension` package remains the behavior reference until this
package reaches parity.

## Commands

- `bun run dev`
- `bun run build`
- `bun run check`
